function Mostrar()
{
	//Genero el número RANDOM entre 1 y 10 
	var valorDado;
	valorDado = Math.floor(Math.random()*10)+1;//aca genere un random de valores enteros en este caso con el signo + hago que vaya del 1 al 10 ambos inclusives
	//valorDado=Math.random()*11; esto es para valores randon con coma 

	alert(valorDado);

}//FIN DE LA FUNCIÓN